import TicketAlert from "@/components/TicketAlerts/TicketAlert";

const TicketAlerts = () => {
  return (
    <div className="pt-[50px]">
      <TicketAlert />
    </div>
  );
};

export default TicketAlerts;
